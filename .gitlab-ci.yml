image: node:26.3.0-alpine

stages:
  - verify

cache:
  key:
    files:
      - pnpm-lock.yaml
  paths:
    - .pnpm-store

verify:
  stage: verify
  before_script:
    - npm install --global pnpm@11.12.0
    - pnpm install --frozen-lockfile
  script:
    - pnpm check
